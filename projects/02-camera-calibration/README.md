# Project 02: Camera Calibration

## Capture Some Chessboard Images

Run
```bash
python capture_images.py --dirname 'chessboard'
```
to start an interactive loop that will display the webcam. Press `<SPACE>` to capture a frame; press `q` to quit.

